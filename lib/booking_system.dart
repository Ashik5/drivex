import 'package:flutter/material.dart';

class BookingSystem extends StatelessWidget {
  const BookingSystem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
